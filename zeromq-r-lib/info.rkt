#lang info

;; ========================================
;; pkg info

(define collection "zeromq")
(define deps
  '(["base" #:version "6.10"]))

;; ========================================
;; collect info

(define name "zeromq")
